import React from 'react';

const FacultyPage = ({ id }) => {
    return (
        <div>
            <h1>Faculty Page</h1>
            <p>This is the page for faculty member with ID: {id}</p>
        </div>
    );
};

export default FacultyPage;